{
  "name": "express-ts",
  "version": "1.0.0",
  "author": "Bartosz Kotrys <bartosz.kotrys@gmail.com>",
  "description": "",
  "repository": "",
  "main": "src/index.ts",
  "private": true,
  "scripts": {
    "preinstall": "npx use-yarn -m 'Please use yarn!'",
    "lint": "tslint -c tslint.json -p tsconfig.json --fix",
    "prebuild": "yarn lint && tsc",    
    "build": "node build/index.js",
    "start": "yarn build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">= 10.0.0 < 11.0.0",
    "yarn": "^1.12.3"
  },
  "keywords": [],
  "license": "ISC",
  "dependencies": {
    "express": "4.16.4"
  },
  "devDependencies": {
    "@types/express": "4.16.0",
    "@types/node": "10.12.12",
    "tslint": "5.11.0",
    "typescript": "3.2.1",
    "use-yarn": "2.2.0"
  }
}
